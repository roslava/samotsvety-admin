/* eslint-disable @typescript-eslint/no-explicit-any */
'use client';
import { UseFormReturn,useFieldArray } from 'react-hook-form'; import { MineralFormData } from '@/lib/validations/mineral'; import { FormField,FormItem,FormLabel,FormControl } from '@/components/ui/form'; import { Input } from '@/components/ui/input'; import { Button } from '@/components/ui/button'; import { Card,CardContent,CardHeader,CardTitle } from '@/components/ui/card';
export function GallerySection({form}:{form:UseFormReturn<MineralFormData>}){const {fields,append,remove}=useFieldArray({control:form.control,name:'images.gallery'}); const ref=(name:string,label:string)=><FormField control={form.control} name={name as any} render={({field})=><FormItem><FormLabel>{label}</FormLabel><FormControl><Input {...field} value={field.value??''}/></FormControl></FormItem>}/>; return <Card><CardHeader><CardTitle>Изображения</CardTitle></CardHeader><CardContent className="space-y-4">{ref('images.storage_key','Storage key')}{ref('images.hero.path','Hero path')}{ref('images.thumbnail.path','Thumbnail path')}<p className="text-xs text-muted-foreground">Путь относительно storage_key. Не повторяйте имя папки.</p>{fields.map((row,i)=><div key={row.id} className="border rounded p-3 space-y-2">{ref(`images.gallery.${i}.path`,`Gallery #${i+1} path`)}{ref(`images.gallery.${i}.caption.ru`,'Caption RU')}{ref(`images.gallery.${i}.caption.en`,'Caption EN')}<Button type="button" variant="destructive" onClick={()=>remove(i)}>Удалить</Button></div>)}<Button type="button" variant="outline" onClick={()=>append({path:'',type:null,caption:{ru:null,en:null}})}>Добавить изображение</Button></CardContent></Card>}
